{
    "features": [
        {
            "id": "httpie",
            "name": "HTTPie for devcontainers",
            "include": ["ubuntu", "debian"],
            "buildArg": "_VSC_INSTALL_DOCKER_IN_DOCKER"
        }
    ]
}
